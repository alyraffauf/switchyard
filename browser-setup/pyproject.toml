[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "switchyard-browser-setup"
version = "0.1.0"
description = "Desktop integration installer for Switchyard"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
browser-setup = "main:main"
